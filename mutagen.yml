sync:
  defaults:
    ignore:
      paths:
        - __pycache__
        - mutagen.yml.lock
        - .esphome
        - tmp
        - .DS_Store

  haos:
    alpha: "./haos"
    beta: "root@192.168.1.32:/homeassistant"
    mode: "two-way-safe"
    ignore:
      paths:
        - /*
        - "!/configuration.yaml"
        - "!/automations.yaml"
        - "!/blueprints"
        - "!/scenes.yaml"
        - "!/scripts.yaml"
        - "!/secrets.yaml"
        - "!/custom_components"
        - "!/esphome"
