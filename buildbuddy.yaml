actions:
  - name: "Test all targets"
    user: buildbuddy
    # NOTE: to be able to install podman?
    container_image: "ubuntu-20.04"
    triggers:
      push:
        branches:
          - "master"
      pull_request:
        branches:
          - "*"
    bazel_commands:
      - "run //:setup_ci"
      # https://github.com/bazelbuild/rules_docker/issues/1663
      - "test --sandbox_writable_path=/run/user/${UID} //..."
