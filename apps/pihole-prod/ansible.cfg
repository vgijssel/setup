[defaults]
pipelining = true
roles_path = ../../libs/ansible/roles
vars_plugins = ../../libs/ansible/vars_plugins

[privilege_escalation]
# Use sudo.ws instead of sudo-rs for Ansible compatibility
# Ubuntu 25.10+ uses sudo-rs which doesn't support Ansible's -p flag
become_exe = sudo.ws
