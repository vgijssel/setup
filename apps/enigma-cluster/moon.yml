language: javascript
layer: application
dependsOn:
  - internal-networking
tags:
  - scope.app
  - type.stack
  - kubernetes
  - production
  - scope.app
  - type.stack
  - kubernetes
  - production
tasks:
  create-1password-secret:
    command: $MOON_WORKSPACE_ROOT/libs/1password-operator-bootstrap/bootstrap.sh --credentials-ref "$CREDENTIALS_REF" --token-ref "$TOKEN_REF"
    env:
      CREDENTIALS_REF: op://enigma-cluster/enigma-cluster Credentials File/1password-credentials.json
      TOKEN_REF: op://enigma-cluster/hxmp2hgt5iluzk5zfq2trx4oae/credential
    local: true
