apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: pr-apps-helm
  namespace: argocd
spec:
  goTemplate: true

  # Progressive Sync strategy with reverse deletion order
  # Applications are deleted in REVERSE order of steps
  strategy:
    type: RollingSync
    deletionOrder: Reverse
    rollingSync:
      steps:
        # Step 1: Infrastructure apps - created FIRST, deleted LAST
        - matchExpressions:
            - key: app-type
              operator: In
              values:
                - infra

        # Step 2: Apps inside vCluster - created LAST, deleted FIRST
        - matchExpressions:
            - key: app-type
              operator: In
              values:
                - apps

  generators:
    - matrix:
        generators:
          - pullRequest:
              github:
                owner: vgijssel
                repo: setup
                labels:
                  - apps:secrets-proxy
                tokenRef:
                  secretName: github-token
                  key: token
              requeueAfterSeconds: 1800
          - git:
              repoURL: https://github.com/vgijssel/setup.git
              revision: "{{.head_sha}}"
              files:
                - path: apps/secrets-proxy/*/config.yaml
                - path: apps/secrets-proxy-infra/*/config.yaml
  template:
    metadata:
      name: "{{.platform}}-{{.appName}}-pr{{.number}}"
      labels:
        pr: "pr{{.number}}"
        app-type: "{{.appType}}"
    spec:
      project: default
      source:
        repoURL: https://github.com/vgijssel/setup.git
        targetRevision: "{{.head_sha}}"
        path: "{{.path.path}}"
        helm:
          valueFiles:
            - values.yaml
          parameters:
            - name: prNumber
              value: "{{.number}}"
            - name: tenant.prNumber
              value: "{{.number}}"
      destination:
        # Use prCluster if set (with printf using full pr suffix), otherwise use cluster directly
        name: '{{if .prCluster}}{{ printf .prCluster (printf "pr%s" .number) }}{{else}}{{.cluster}}{{end}}'
        namespace: '{{if eq .prUpdateNamespace true}}{{.namespace}}pr{{.number}}{{else}}{{.namespace}}{{end}}'
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - PruneLast=true
      info:
        - name: PR
          value: "https://github.com/vgijssel/setup/pull/{{.number}}"
        - name: Branch
          value: "{{.branch}}"
  templatePatch: |
    {{- if .createNamespace }}
    spec:
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
    {{- end }}
    {{- if .prOverrides }}
    spec:
      source:
        helm:
          parameters:
            {{- range $key, $formatString := .prOverrides }}
            - name: {{ $key }}
              value: '{{ printf $formatString (printf "pr%s" $.number) }}'
            {{- end }}
    {{- end }}
