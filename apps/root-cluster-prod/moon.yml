language: javascript
layer: application
tags:
  - scope.app
  - type.stack
  - kubernetes
  - production
  - scope.app
  - type.stack
  - kubernetes
  - production
tasks:
  create-1password-secret:
    command: $MOON_WORKSPACE_ROOT/libs/1password-operator-bootstrap/bootstrap.sh --credentials-ref "$CREDENTIALS_REF" --token-ref "$TOKEN_REF"
    env:
      CREDENTIALS_REF: op://root-cluster-prod/root-cluster-prod Credentials File/1password-credentials.json
      TOKEN_REF: op://root-cluster-prod/gfyeeek3sr5n4geqyj7ic2gkwi/credential
    options:
      runInCI: false
      interactive: true
