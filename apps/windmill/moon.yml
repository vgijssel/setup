id: windmill
language: javascript
layer: application
tags:
  - scope.app
  - type.service
  - platform.kubernetes
  - scope.app
  - type.service
  - platform.kubernetes
tasks:
  login:
    command: task login
    env:
      CWD: apps/windmill
    toolchain: system
  sync:
    command: task sync
    env:
      CWD: apps/windmill
    toolchain: system
toolchain:
  default: node
