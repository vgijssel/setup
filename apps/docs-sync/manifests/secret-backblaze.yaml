apiVersion: v1
kind: Secret
metadata:
  name: backblaze-credentials
  namespace: relay
stringData:
  bucket: s3-docs-infra-prod
  # Empty credentials - let aws-sigv4-proxy handle signing
  key_id: ""
  application_key: ""
  # Cross-cluster access via stable service alias on host cluster
  endpoint: http://aws-sigv4-proxy-secrets.tenant-prod.svc.cluster.local
  region: us-west-004
