apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: setup
  namespace: cozy-fluxcd
spec:
  interval: 5m
  url: https://github.com/vgijssel/setup
  ref:
    branch: feat/relay-server-backblaze-storage
