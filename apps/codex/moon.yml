id: codex
language: javascript
layer: application
tags:
  - scope.app
  - type.tooling
  - ci
tasks:
  setup:
    command: "./setup.sh"
    local: true
  test:
    command: bash -c './setup.sh && command -v direnv && direnv --version'
    inputs:
      - glob: "**/*"
        cache: true
    options:
      cache: true
