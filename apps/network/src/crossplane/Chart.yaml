apiVersion: v2
name: crossplane
description: Crossplane core — the reconciler that owns the tailnet policy (replaces terranetes in apps/network)
version: 1.0.0
appVersion: 2.3.3
# Fleet resolves this file:// dependency against the vendir-vendored chart at
# bundle-build time (helm dependency update equivalent); no committed charts/*.tgz.
dependencies:
  - name: crossplane
    version: 2.3.3
    repository: file://../../../../third_party/vendir/charts/crossplane
