# The build-time-baked uosserver.service must wait for the first-boot image load (which also
# ensures the graphroot bind onto the Volume + lingering are in place) before it starts the
# rootless UOS container.
[Unit]
After=uosserver-load.service
Requires=uosserver-load.service
