# Runtime OpenTofu artifacts. State lives ONLY in the kubernetes-backend Secret
# terraform-controller injects (inside the child), never in git. .terraform.lock.hcl
# is intentionally NOT ignored — it pins the providers for reproducible inits.
.terraform/
*.tfstate
*.tfstate.*
crash.log
