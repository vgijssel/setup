apiVersion: v2
name: ingress-nginx
description: ingress-nginx controller (ClusterIP-only) for in-cluster Ingress; OpenBao is now fronted by the NetBird BYOP reverse proxy
version: 1.0.0
appVersion: 1.12.0
dependencies:
  - name: ingress-nginx
    version: 4.12.0
    repository: file://../../../../third_party/vendir/charts/ingress-nginx
