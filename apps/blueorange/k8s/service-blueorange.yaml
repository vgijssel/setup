apiVersion: v1
kind: Service
metadata:
  name: blueorange
  namespace: blueorange
  labels:
    app: blueorange
spec:
  selector:
    app: blueorange
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
