apiVersion: v1
kind: Service
metadata:
  name: blueorange
  labels:
    app: blueorange
spec:
  type: ClusterIP
  selector:
    app: blueorange
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    name: http
