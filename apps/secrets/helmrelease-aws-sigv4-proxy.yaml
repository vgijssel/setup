apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: aws-sigv4-proxy
  namespace: flux-system
spec:
  kubeConfig:
    secretRef:
      name: k3k-secrets-kubeconfig
      key: kubeconfig.yaml
  interval: 30m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: ./libs/aws-sigv4-proxy
      sourceRef:
        kind: GitRepository
        name: setup
        namespace: flux-system
      interval: 12h
      reconcileStrategy: Revision
  targetNamespace: secrets
  dependsOn:
    - name: 1password-secrets
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    service:
      type: ClusterIP
    aws:
      secretName: backblaze-s3-docs-infra-prod
