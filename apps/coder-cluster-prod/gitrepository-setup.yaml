apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: coder-cluster-prod
  namespace: flux-system
spec:
  interval: 5m
  url: https://github.com/vgijssel/setup
  ref:
    branch: mg/feat/enable-linstor-network
