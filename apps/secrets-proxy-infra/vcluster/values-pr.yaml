# PR environment overrides for secrets-proxy vcluster
vcluster:
  controlPlane:
    statefulSet:
      highAvailability:
        replicas: 1
    backingStore:
      etcd:
        deploy:
          statefulSet:
            highAvailability:
              replicas: 1
    ingress:
      enabled: false
