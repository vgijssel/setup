language: javascript
layer: application
tags:
  - type.application
  - scope.home-automation
  - platform.kubevirt
  - cluster.enigma
tasks:
  bootstrap:
    command: kubectl apply -k $MOON_WORKSPACE_ROOT/apps/haos-enigma/
    options:
      runInCI: false
