apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: relay
  namespace: tenant-root
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    # WebSocket support - HTTP/1.1 required for connection upgrade
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    # Disable buffering for real-time WebSocket/streaming connections
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    # Extended timeouts to prevent WebSocket disconnections (default 60s)
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
spec:
  ingressClassName: tenant-root
  tls:
    - hosts:
        - relay.enigma.vgijssel.nl
      secretName: relay-tls
  rules:
    - host: relay.enigma.vgijssel.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                # PLACEHOLDER: This service name will be updated after bootstrap
                # when the KubeVirt CCM creates the mirrored LoadBalancer service
                # in the enigma-cluster tenant-root namespace
                name: PLACEHOLDER_RELAY_LB_SERVICE
                port:
                  number: 8080
