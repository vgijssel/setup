#cloud-config

chpasswd: { expire: False }
# enable password based ssh logins
ssh_pwauth: True

users:
  # TODO: set /bin/bash as default shell instead of /bin/sh
  - name: packer
    plain_text_passwd: packer
    lock_passwd: false
    sudo: ALL=(ALL) NOPASSWD:ALL
