---
driver:
  name: vagrant
  vagrantfiles:
    - <%= ENV.fetch('VAGRANTFILE_CLOUD_INIT') %>

platforms:
  - name: ubuntu-focal
    driver:
      box: ubuntu-focal
      box_url: file:///Users/maarten/Development/setup/bazel-out/darwin-fastbuild/bin/hypervisor/hypervisor_image_box.box

# platforms: # a list of OS on which we are going to run tests
#   - name: centos-7.2 # our recipes
#     driver:
#       customize:
#         memory: 2048 # configurations for the given OS
verifier:
  name: inspec

# provisioner:
#   chef_license: accept

suites:
  - name: default
    verifier:
      inspec_tests:
        - test/integration/default
    attributes:
