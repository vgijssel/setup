SCRIPT = "//tools/qcow_to_vagrant:qcow_to_vagrant_script"

def qcow_to_vagrant(name:str, src:str):
    return genrule(
        name = name,
        srcs = [src],
        outs = [name + '.box'],
        cmd = """
        $TOOLS_SCRIPT "$SRCS" "$OUT"
        """,
        tools = {
            "script": SCRIPT,
        }
    )
