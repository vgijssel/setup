load("//tools/command:command.bzl", "command")

package(default_visibility = ["//visibility:public"])

command(
    name = "terraform",
    command_src = "@terraform_arm64//:terraform_binary",
    cwd = "{{ os.environ.get('BUILD_WORKSPACE_DIRECTORY', os.getcwd()) }}",
)
