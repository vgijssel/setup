load("@buildifier_prebuilt//:rules.bzl", "buildifier_test")

buildifier_test(
    name = "test",
    exclude_patterns = [
        "./.git/*",
    ],
    lint_mode = "warn",
    no_sandbox = True,
    workspace = "@@//:WORKSPACE",
)
