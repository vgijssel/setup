load("@buildifier_prebuilt//:rules.bzl", "buildifier_test")

buildifier_test(
    name = "test",
    timeout = "short",
    srcs = ["//:bazel-files"],
    lint_mode = "warn",
    # NOTE: without this it fails in BuildBuddy without any explanation.
    tags = [
        "no-remote-exec",
    ],
    verbose = True,
)
