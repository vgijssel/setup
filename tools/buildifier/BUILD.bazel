load("@buildifier_prebuilt//:rules.bzl", "buildifier_test")

buildifier_test(
    name = "test",
    srcs = ["BUILD.bazel"],
    exclude_patterns = [
        "./.git/*",
    ],
    lint_mode = "warn",
    no_sandbox = True,
    verbose = True,
    workspace = "@setup//:WORKSPACE",
)
