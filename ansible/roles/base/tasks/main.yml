- name: Ensure a locale exists kerk
  community.general.locale_gen:
    name: en_US.UTF-8
    state: present
  become: true

- name: Copy locale settings
  ansible.builtin.copy:
    src: locale
    dest: /etc/default/locale
    owner: root
    group: root
    mode: "0644"
  become: true
