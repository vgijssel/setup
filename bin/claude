#!/bin/bash
# Binstub for Claude Code CLI

LOCAL_SETUP_DIR="$(realpath "$(dirname "$(realpath "$0")")/.." || true)"
EXEC_BIN="${LOCAL_SETUP_DIR}"/bin/exec

exec ${EXEC_BIN} npx --yes --prefix "${LOCAL_SETUP_DIR}"/third_party/javascript @anthropic-ai/claude-code "$@"
