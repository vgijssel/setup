#!/bin/bash
# Binstub for vault-login CLI tool

set -eo pipefail

LOCAL_SETUP_DIR="$(realpath "$(dirname "$(realpath "$0")")/.." || true)"
EXEC_BIN="${LOCAL_SETUP_DIR}"/bin/run

exec ${EXEC_BIN} uv run "${LOCAL_SETUP_DIR}"/libs/vault-login/vault-login.py "$@"
