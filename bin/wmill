#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(realpath "$(dirname "$(realpath "$0")")/.." || true)"
EXEC_BIN="${REPO_ROOT}"/bin/run

exec "${EXEC_BIN}" moon run --interactive --quiet --log error windmill-cli:exec -- wmill "$@"
