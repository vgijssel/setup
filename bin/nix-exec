#!/usr/bin/env bash

set -Eeou pipefail

export PATH=$(pwd)/dist/delegator:$PATH

exec delegator \
    --name nix-exec \
    --volume $HOME \
    --volume $(realpath $TMPDIR) \
    --image image-builder:dev \
    "$@"