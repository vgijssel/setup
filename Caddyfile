# Bind only inside the workspace container
:8123 {
	reverse_proxy http://192.168.1.32:8123 {
		# Set Host header to the Home Assistant server
		header_up Host 192.168.1.32:8123

		# Required for WebSocket connections
		header_up Upgrade {>Upgrade}
		header_up Connection {>Connection}
	}