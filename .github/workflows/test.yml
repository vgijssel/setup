name: Test
on: [push]

jobs:
  image_builder:
    name: Build image
    env:
      IMAGE_BUILDER_IMAGE: image-builder
      IMAGE_BRANCH_TAG: ${{ github.ref }}
      IMAGE_SHA_TAG: ${{ github.sha }}
      DOCKER_BUILDKIT: 1
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - env:
        GITHUB: ${{ toJson(github) }}
        ENV: ${{ toJson(env) }}
        JOB: ${{ toJson(job) }}
        STEPS: ${{ toJson(steps) }}
        RUNNER: ${{ toJson(runner) }}
        SECRETS: ${{ toJson(secrets) }}
        STRATEGY: ${{ toJson(strategy) }}
        MATRIX: ${{ toJson(matrix) }}
      run: |
        env

    - name: Login
      run: |
        docker login -u mvgijssel -p ${{ secrets.GITHUB_TOKEN }} docker.pkg.github.com

    - name: Build the image builder
      run: |
        docker pull ${IMAGE_BUILDER_IMAGE}:latest || true
        docker pull ${IMAGE_BUILDER_IMAGE}:${IMAGE_BRANCH_TAG} || true

        echo ${IMAGE_BUILDER_IMAGE}:${IMAGE_BRANCH_TAG}
        echo ${IMAGE_BUILDER_IMAGE}:${IMAGE_SHA_TAG}


        docker build \
          --tag ${IMAGE_BUILDER_IMAGE}:${IMAGE_BRANCH_TAG} \
          --tag ${IMAGE_BUILDER_IMAGE}:${IMAGE_SHA_TAG} \
          --file kubernetes/Dockerfile \
          kubernetes

        docker push ${IMAGE_BUILDER_IMAGE}:${IMAGE_BRANCH_TAG}
        docker push ${IMAGE_BUILDER_IMAGE}:${IMAGE_SHA_TAG}

    - name: Run
      if: always()
      run: |
        docker run \
          --rm \
          --privileged \
          --volume $PWD/kubernetes/images:/app \
          --volume $PWD/kubernetes/elements:/elements \
          --env DIB_RELEASE=stretch \
          --env DIB_APT_MINIMAL_CREATE_INTERFACES=0 \
          ${IMAGE_BUILDER_IMAGE}:${IMAGE_SHA_TAG} \
          env

    - name: Run
      if: always()
      run: |
        docker run \
          --rm \
          --privileged \
          --volume $PWD/kubernetes/images:/app \
          --volume $PWD/kubernetes/elements:/elements \
          --env DIB_RELEASE=stretch \
          --env DIB_APT_MINIMAL_CREATE_INTERFACES=0 \
          ${IMAGE_BUILDER_IMAGE}:${IMAGE_SHA_TAG} \
          disk-image-create -o stretch_1 debian vm debian-networking-fix cloud-init-fix kubernetes growroot qemu-guest nfs resolvconf goss

    # - name: Test kvm
    #   if: always()
    #   run: kvm-ok

    # - name: Test CPU info
    #   if: always()
    #   run: egrep -c '(vmx|svm)' /proc/cpuinfo

    # - name: Test CPU info
    #   if: always()
    #   run: cat /sys/hypervisor/properties/capabilities

  test1:
    name: Test 1
    runs-on: macos-latest
    steps:

    - name: Checkout
      uses: actions/checkout@v1

    - name: Test action
      if: always()
      uses: ./

    - name: Test qemu
      if: always()
      run: |
        qemu-img --help

    - name: Test brew
      if: always()
      run: |
        brew info

    - name: Test docker
      if: always()
      run: |
        docker info

    - uses: actions/checkout@v1
      with:
        ref: ${{ matrix.ref }}
