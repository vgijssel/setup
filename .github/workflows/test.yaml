name: Pull Request
on: [pull_request]
concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions: read-all

jobs:
  trunk_check:
    name: Trunk Check Runner
    runs-on: ubuntu-latest
    permissions:
      checks: write # For trunk to post annotations
      contents: read # For repo checkout

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Trunk Check
        uses: trunk-io/trunk-action@v1

  # TODO: implement this in nx
  # docs-build:
  #   name: docs:build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v3
  #     - name: Init Hermit
  #       uses: cashapp/activate-hermit@v1
  #     - uses: HatsuneMiku3939/direnv-action@v1
  #       with:
  #         direnvVersion: 2.35.0
  #     - run: sudo apt install -y libjpeg-turbo-progs
  #     - run: task docs:build

  # TODO: implement this in nx
  #   codex-test:
  #     name: stacks:codex:test
  #     runs-on: ubuntu-latest
  #     steps:
  #       - name: Checkout
  #         uses: actions/checkout@v3
  #       - name: Init Hermit
  #         uses: cashapp/activate-hermit@v1
  #       - run: task stacks:codex:test

  test-linux:
    name: Linux Tests (All Projects)
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup
      - run: nx run-many -t lint test build --exclude=tag:macos

  test-macos:
    name: macOS Tests (Tagged Projects Only)
    runs-on: macos-latest
    steps:
      - uses: ./.github/actions/setup
      - run: nx run-many -t lint test build --projects=tag:macos
