# NOTE: no -e flag here, because that will result in a kernel panic
set -Eoux pipefail

echo "Start remote disk deploy"

DISK_URL=$(get_kernel_parameter DISK_URL)

echo "Starting to download disk: $DISK_URL"

curl -v -L $DISK_URL -o disk.qcow2

ls -lh
