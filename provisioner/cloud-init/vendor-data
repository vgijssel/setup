#cloud-config

manage_etc_hosts: localhost
hostname: provisioner

groups:
  - docker: [ubuntu]

# users:
#   - name: ubuntu
#     lock_passwd: false
#     shell: /bin/bash
#     sudo: ALL=(ALL) NOPASSWD:ALL
