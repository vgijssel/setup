load("@rules_task//:defs.bzl", "task")
load("@pip//:requirements.bzl", "requirement")

task(
    name = "hello",
    cmds = [
        "echo Hello, world!",
    ],
)

py_test(
    name = "test",
    srcs = [
        "pytest_wrapper.py",
        "test.py",
    ],
    args = [
        "-ra",
        "-vv",
    ] + ["$(location :%s)" % x for x in ["test.py"]],
    data = [
        ":hello",
    ],
    main = "pytest_wrapper.py",
    deps = [
        requirement("bazel-runfiles"),
        requirement("tomli"),
        requirement("pytest"),
    ],
)
