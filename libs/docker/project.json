{
  "name": "docker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/docker",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bazel build //libs/docker:all",
        "cwd": "."
      },
      "cache": true,
      "inputs": ["{projectRoot}/**/*"],
      "metadata": {
        "description": "Build Docker Bazel rules"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bazel test //libs/docker:all",
        "cwd": "."
      },
      "cache": true,
      "inputs": ["{projectRoot}/**/*"],
      "metadata": {
        "description": "Test Docker Bazel rules"
      }
    }
  },
  "tags": ["scope:lib", "type:tool"]
}
