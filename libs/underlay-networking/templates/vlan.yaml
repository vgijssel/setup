apiVersion: kubeovn.io/v1
kind: Vlan
metadata:
  name: {{ .Values.vlan.name }}
  labels:
    {{- include "underlay-networking.labels" . | nindent 4 }}
spec:
  id: {{ .Values.vlan.id }}
  provider: {{ .Values.providerNetwork.name }}
