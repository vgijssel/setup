language: javascript
layer: library
tags:
  - coder
  - devcontainer
  - terraform
  - development-environment
tasks:
  init:
    command: terraform init -backend=false
    options:
      cache: true
  test:
    command: terraform validate
    deps:
      - ~:init
    options:
      cache: true
