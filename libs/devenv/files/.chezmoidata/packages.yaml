packages:
  darwin:
    taps:
      - coder/coder
    brews:
      - git
      - zsh
      - httpie
      - coreutils
      - gnu-sed
      - curl
      - wget
      - fontconfig
    casks:
      - google-chrome
      - orbstack
      - iterm2
      - visual-studio-code
      - font-fira-code-nerd-font
      - alfred
      - whatsapp
      - spotify
      - todoist
      - raspberry-pi-imager
      - balenaetcher
      - steam
      - 1password
      - discord
      - slack
      - obsidian
      - gimp
      - chatgpt
      - claude
      - github
      - finicky
      - obs
      - coder/coder/coder-desktop
      - motion
      - resilio-sync
  linux:
    apt_packages:
      - git
      - zsh
      - ca-certificates
      - curl
      - wget
      - ssh
      - busybox
      - bash
      - sudo
      - locales
      - bash
      - fontconfig
      - unzip
      # NOTE: adding jq here because the `git-commit-signing` module
      # needs it during setup and we currently don't have a way to
      # control install order of coder modules.
      # So we're not sure if Hermit jq's is available when the module runs.
      # See: https://github.com/coder/coder/issues/10352
      - jq
      - dnsutils # Provides nslookup for DNS testing in chainsaw tests
