{
  "name": "devenv",
  "projectType": "library",
  "sourceRoot": "libs/devenv",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["chezmoi apply --force --verbose", "goss validate"],
        "parallel": false,
        "cwd": "libs/devenv"
      },
      "cache": true,
      "metadata": {
        "description": "Run goss validation tests for the devenv library"
      }
    }
  },
  "tags": ["devenv", "chezmoi", "development-environment", "macos"]
}
