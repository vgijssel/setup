---
# OTBR Role Handlers

- name: reload sysctl
  ansible.builtin.command:
    cmd: sysctl --system
  become: true

- name: reload udev
  ansible.builtin.shell:
    cmd: udevadm control --reload-rules && udevadm trigger
  become: true
