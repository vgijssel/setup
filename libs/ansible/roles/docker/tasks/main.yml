---
# Docker role main tasks
# Installs Docker and configures daemon settings

- name: Install Docker
  ansible.builtin.import_tasks: install.yml
  tags: docker-install

- name: Configure Docker daemon
  ansible.builtin.import_tasks: configure.yml
  tags: docker-configure
