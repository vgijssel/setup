language: javascript
layer: library
tags:
  - type.lib
  - scope.tools
  - 1password
tasks:
  start:
    command: uv run vault-shell.py
    local: true
  test:
    command: uv run test_vault_shell.py
    inputs:
      - glob: "*.py"
        cache: true
    options:
      cache: true
