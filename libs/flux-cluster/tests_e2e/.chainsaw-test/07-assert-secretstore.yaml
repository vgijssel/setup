# Assert SecretStore is created
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: tenant-test-store
  namespace: tenant-test
spec:
  provider:
    kubernetes:
      remoteNamespace: default
      authRef:
        name: k3k-test-cluster-secrets-kubeconfig
        key: kubeconfig.yaml
