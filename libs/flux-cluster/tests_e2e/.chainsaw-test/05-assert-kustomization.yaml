# Assert Kustomization is created in the FluxCluster namespace
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: test-cluster
  namespace: tenant-test
spec:
  interval: 30m
  sourceRef:
    kind: GitRepository
    name: setup-test-cluster
  path: ./apps/docs-sync
  prune: true
  wait: true
  timeout: 10m
