# Assert k3k Cluster resource has addon reference for DNS forwarding
apiVersion: k3k.io/v1beta1
kind: Cluster
metadata:
  name: test-cluster-dns
  namespace: tenant-test
spec:
  addons:
    - secretNamespace: tenant-test
      secretRef: test-cluster-dns-coredns-addon
