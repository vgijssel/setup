type: application
language: yaml
tags:
  - kubernetes
  - kro
  - flux
  - kind
tasks:
  test:
    command: dev-cluster create flux-cluster-test --flux-path ./apps/flux-system && chainsaw test --test-dir tests_e2e --kube-context kind-flux-cluster-test && dev-cluster delete flux-cluster-test
    inputs:
      - resourcegroup.yaml
      - kustomization.yaml
      - tests_e2e/**/*
    options:
      cache: true
