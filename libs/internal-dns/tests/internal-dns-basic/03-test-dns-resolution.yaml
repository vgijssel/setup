apiVersion: kuttl.dev/v1beta1
kind: TestStep
metadata:
  name: test-dns-resolution
spec:
  commands:
  # Wait for external-dns to process the ingress
  - command: sleep 60
  # Check external-dns logs to see if it's processing
  - command: kubectl logs deployment/internal-dns-test-external-dns --tail=10