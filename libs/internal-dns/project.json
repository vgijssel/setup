{
  "name": "internal-dns",
  "projectType": "library",
  "sourceRoot": "libs/internal-dns",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "kuttl test",
        "cwd": "libs/internal-dns",
        "start-kind": false,
        "skip-cluster-delete": true,
        "configurations": {
          "CI": {
            "start-kind": true,
            "skip-cluster-delete": false
          }
        }
      },
      "cache": true,
      "inputs": [
        "{projectRoot}/**/*",
        "!{projectRoot}/kind-logs-*/**/*",
        "!{projectRoot}/kubeconfig",
        { "env": "NX_TASK_TARGET_CONFIGURATION" }
      ],
      "outputs": []
    }
  },
  "tags": ["type:library", "scope:infrastructure", "platform:kubernetes"]
}
