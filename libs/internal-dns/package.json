{
  "name": "internal-dns",
  "version": "1.1.0",
  "type": "commonjs",
  "nx": {
    "projectType": "library",
    "sourceRoot": "libs/internal-dns",
    "targets": {
      "test": {
        "executor": "nx:run-commands",
        "options": {
          "commands": [
            "dev-cluster create internal-dns-test",
            "chainsaw test --test-dir chainsaw-test --config chainsaw-test.yaml --kube-context kind-internal-dns-test",
            "dev-cluster delete internal-dns-test"
          ],
          "parallel": false,
          "cwd": "libs/internal-dns"
        },
        "cache": true,
        "inputs": [
          "{projectRoot}/**/*",
          "!{projectRoot}/kind-logs-*/**/*"
        ],
        "outputs": []
      }
    },
    "tags": [
      "type:library",
      "scope:infrastructure",
      "platform:kubernetes"
    ]
  },
  "private": true
}
