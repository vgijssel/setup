apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Configuration
metadata:
  name: internal-dns-test-config
spec:
  timeouts:
    apply: 2m
    assert: 1m
    cleanup: 1m
    delete: 1m
    error: 30s
    exec: 15m
