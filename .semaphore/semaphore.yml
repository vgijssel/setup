version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Build docker base image
    task:
      jobs:
        - name: checkout
          commands:
            - checkout
        - name: load env
          commands:
            - export KERK=1
            - env
        - name: docker build
          commands:
            - env
    dependencies: []
