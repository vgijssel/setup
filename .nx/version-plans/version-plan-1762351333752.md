---
fleet-mcp: minor
---

feat: Add display_name support for project endpoints

Allow create_agent and list_agent_roles endpoints to accept both project
template names (e.g., "coder-devcontainer") and user-friendly display names
(e.g., "Setup"). Fully backwards compatible with existing code.
