layer: library
tags:
  - javascript
  - pnpm
tasks:
  test:
    command: pnpm install --frozen-lockfile
    inputs:
      - glob: tools/*/package.json
      - file: /pnpm-lock.yaml
      - file: /pnpm-workspace.yaml
    options:
      cache: true
      mutex: pnpm-install
