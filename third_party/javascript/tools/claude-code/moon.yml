layer: library
tags:
  - javascript
  - npm
tasks:
  install:
    command: pnpm install --filter .
    inputs:
      - file: package.json
      - file: /pnpm-lock.yaml
    outputs:
      - node_modules
    options:
      cache: true
      mutex: pnpm-install

  start:
    command: pnpm exec claude
    deps:
      - ~:install
    local: true
