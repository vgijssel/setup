; Please config file
; Leaving this file as is is enough to use plz to build your project.
; Please will stay on whatever version you currently have until you run
; 'plz update', when it will download the latest available version.
;
; Or you can uncomment the following to pin everyone to a particular version;
; when you change it all users will automatically get updated.
; [please]
; version = 16.2.1

[buildconfig]
; default-docker-repo = hub.docker.com
default-docker-repo = localhost
ssh-config-dir = tmp/vagrant/ssh_configs

[python]
moduledir = third_party.python

[build]
; to make sure we are using gnu-sed and not bsd sed
; so we can make use of the pleasings terraform build defs
path = /usr/local/opt/gnu-sed/libexec/gnubin

; This is the default please PATH variable
path = /usr/local/bin:/usr/bin:/bin

[alias "ssh"]
cmd = run //tools/ssh --

[Parse]
PreloadBuildDefs = ./tools/please/sh_rules.build_defs