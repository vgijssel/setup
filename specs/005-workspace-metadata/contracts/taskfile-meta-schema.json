{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://setup.internal/schemas/taskfile-meta-v1.json",
  "title": "Taskfile Meta Key Schema",
  "description": "Schema for the 'meta' key in Taskfile tasks that define workspace metadata fields",
  "type": "object",
  "properties": {
    "meta": {
      "type": "object",
      "description": "Metadata configuration for tasks that collect workspace information",
      "properties": {
        "include_in_list": {
          "type": "boolean",
          "description": "Whether this metadata field should be included in list_agents responses (true) or only in show_agent responses (false)"
        }
      },
      "required": ["include_in_list"],
      "additionalProperties": false
    }
  },
  "required": ["meta"],
  "examples": [
    {
      "meta": {
        "include_in_list": true
      }
    },
    {
      "meta": {
        "include_in_list": false
      }
    }
  ]
}
