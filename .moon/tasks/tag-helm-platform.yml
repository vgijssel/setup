$schema: ../cache/schemas/tasks.json

tasks:
  generate:
    command: helm-platform generate
    inputs:
      - "**/*"
      - "**/config.yaml"
    outputs:
      - "**/config.yaml"
    options:
      cache: true

  validate:
    command: helm-platform validate
    inputs:
      - "**/*"
    options:
      cache: true
